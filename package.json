{
  "name": "restock-reminder",
  "version": "1.0.0",
  "description": "Smart reorder reminders for Shopify stores - boost repeat purchases automatically",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "npx prisma generate && npx prisma db push --accept-data-loss",
    "worker": "node worker.js"
  },
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "@shopify/shopify-api": "^11.0.0",
    "compression": "^1.8.0",
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "node-cron": "^3.0.3",
    "nodemailer": "^6.9.16",
    "prisma": "^6.5.0"
  }
}
